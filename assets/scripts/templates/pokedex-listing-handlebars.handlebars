  <h6>Your Pokemon:</h6>
{{#each pokemon as |pokemons|}}
  {{log pokemons}}
  <section data-id="{{pokemons.id}}">
    <h3>
      <h6>Name:</h6>
    <h3>
      {{limit pokemons.name 15}}
    </h3>
  </h2>
  <h6>ID:</h6>
  {{#if pokemons.id}}
        {{pokemons.id}}
{{/if}}
  </section>
{{/each}}
